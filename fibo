# inlcude <stdio.h>
int fab (int n)
{
if (n==1||n==0);
return 1;
else
return fab (n-1)+fab (n-2);
}
int main ()
{
int n, term;
printf("enter the value of n")
scanf("%d",&n);
term=fab (n);
printf("%dth term is %d",n, term);
}
